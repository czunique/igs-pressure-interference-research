# 原始数据与复现

按用户要求不归档SGY、原始Excel监测数据、原始曲线缓存、地质设计原文提取缓存、运行环境及第三方参考文献全文。results中的结果表、图件和标注为研究派生成果，仍可能含现场井名，因此仓库设为私有。

experiments保留历史代码和目录结构，脚本中的E盘和F盘绝对路径暂按原实验环境保留。缺少本地原始资料和部分缓存时，不能在新机器一键完整重算。先按各阶段说明从原始资料重建过程，再运行分类预测。分类预测所需包版本见其audit.json。表格/Word构建还依赖原Codex运行环境，不能将其视为公共pip依赖。

推荐阅读顺序：V3形态报告、地质工程因素方法、分类预测结果说明、中文IGS初稿。历史各轮结果口径不同，以相应报告为准，不直接拼接指标。

对话记录为摘要，不是完整逐字导出。原始资料始终保留本地且未改动。
