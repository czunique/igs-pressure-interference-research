# 本轮复现

科学计算环境沿用时序聚类_20260915/venv/Scripts/python.exe；已安装xgboost 3.4.1和shap 0.52.0。版本与输入SHA256见audit.json。

依次运行train_models.py、figures.py、verify_and_package.py。build_workbook.mjs使用已配置的artifact-tool与bundled Node；build_manuscript.py使用bundled Python，render_qa.ps1用Word只读导出做排版核查；final_checks.py核对Excel。

原始输入在地质工程因素_20260916/pair_factors.pkl。模型输入model_inputs.pkl已经完成上游字段质控，缺失值尚未插补。新数据必须按clean函数同样处理且按FEATURES顺序供给；不能直接把原始Excel的任意列送入模型。

final_pipeline.joblib与train_models.py放在可导入的同一目录。XGBoost_final.ubj只含树模型，不含列筛选、插补、标准化；使用pipeline才包含完整预处理。

训练脚本直接运行产生的FoldColumns类由verify_and_package.py重新保存为可导入模块类。所有fold模型均校验重载概率。最终模型用全量主实验样本拟合，仅用于后续探索性调用；其训练拟合不能视为新的验证性能。

training.log为发现跨平台关联之前的初查日志，最终结果以training_corrected.log、audit.json、frozen_splits.json和结论目录CSV为准。两次运行之间仅作结构性纠正：连通平台分组及来源异常垂向应力列剔除，未搜索随机种子挑选高分。
